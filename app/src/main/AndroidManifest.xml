<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ff.material">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".core.BottomNavigationViewActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".core.MaterialDesignActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".core.ToolbarActivity"
            android:label="ToolbarActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".core.MovieDetailActivity"
            android:theme="@style/MovieDetailActivityTheme" />
        <activity
            android:name=".core.FloatTabActivity"
            android:label="美团"
            android:theme="@style/FloatActivityTheme" />
        <activity
            android:name=".core.VipActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>